# Adagio
 MusicAIRecognition

To run the Program you need to open the XAMPP
start the Apache and MySQL
dont forget to configure the phpmyadmin
to run you need to type node server.js

Req :
https://nodejs.org/en
"npm ini"
"npm install express multer"
"npm install mysql"
"npm install acrcloud"
"npm install cors"
"npm install fluent-ffmpeg"

More Updated are coming soon !
